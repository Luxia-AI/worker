# CPU-only PyTorch (much smaller than CUDA version)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.9.1+cpu

# Core dependencies
pinecone
pydantic
pydantic_settings
sentence_transformers
trafilatura
playwright
groq
neo4j
fastapi
uvicorn[standard]
websockets
redis
aiofiles
httpx
python-dotenv
aiokafka

# Local LLM support (CPU-only, for fallback after Groq quota)
llama-cpp-python
